# Agent Rules

- **FFF MCP Preference**: Always prefer and use tools from the `fff` MCP server (`find_files`, `grep`, `multi_grep`) when searching for files, finding file paths, or looking up text/patterns within the workspace, rather than using generic shell commands or other native search tools.
